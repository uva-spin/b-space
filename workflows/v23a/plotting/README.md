# v23a plotting

Publication-style b-space, k-space, and 3-D plotting drivers.  They consume
completed tables rather than fitting models.  Keep flavor, `x`, `Q`, band
definition, transform regularization, and source paths in the figure-side
metadata so the plot can be regenerated exactly.
