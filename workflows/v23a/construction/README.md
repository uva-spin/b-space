# v23a construction

Replica-plan, PDF-overlay, b-space aggregation, and regularized k-space
constructors.  The b-space table is primary; the k-space constructors require
an explicit tail mode, finite transform range, and taper.  Record those
settings in the output manifest before using a plot.
