# v23a utilities

Reserved for non-fitting v23a helper drivers.  Any new utility should record
its input manifest, output location, and whether it is an audit, constructor,
or publication helper; large generated artifacts remain outside the source
repository.
