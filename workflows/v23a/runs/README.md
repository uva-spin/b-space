# v23a fit runs

Fixed-target cache, central-refit, absolute-normalization, and grid/audit
drivers.  Launch from the repository root with a fresh `OUT` directory and
the exact `DATA_DIR`, `TRAIN`, `BACKEND`, and cache metadata for the candidate.
Never reuse a cache generated for a different data or nuisance convention.
