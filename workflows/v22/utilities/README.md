# v22 utilities

Cache export, external-benchmark inventory/reduction, and anchor-selection
helpers.  These utilities connect large external artifacts to the public
source release; they do not contain those artifacts and do not modify frozen
production files.
