# v22 construction

Constructors for row-aligned b-space central grids and replica bands.  Inputs
must come from a completed, audited fit and compatible backend cache.  Choose
a new `--out` directory; do not point a diagnostic constructor at the frozen
production directory.
